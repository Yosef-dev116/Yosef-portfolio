"use client";
import {useEffect,useState} from "react";
import {Menu,X,Sun,Moon,Command,ArrowUp} from "lucide-react";
import {motion,useScroll,useSpring} from "motion/react";
const links=["about","skills","projects","experience","github","blog","contact"];
export default function SiteShell({children}:{children:React.ReactNode}){const[open,setOpen]=useState(false);const[light,setLight]=useState(false);const[palette,setPalette]=useState(false);const{scrollYProgress}=useScroll();const scaleX=useSpring(scrollYProgress,{stiffness:140,damping:30});useEffect(()=>{document.documentElement.classList.toggle("light",light)},[light]);useEffect(()=>{const h=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setPalette(v=>!v)}};addEventListener("keydown",h);return()=>removeEventListener("keydown",h)},[]);return <><motion.div style={{scaleX}} className="fixed left-0 right-0 top-0 z-[80] h-[2px] origin-left bg-violet-400"/><header className="fixed top-4 z-50 w-full px-4"><nav className="glass container flex h-14 items-center justify-between rounded-2xl px-4" aria-label="Primary"><a href="#top" className="font-bold tracking-tight focus-ring">YM<span className="text-violet-400">.</span></a><div className="hidden md:flex items-center gap-5">{links.map(x=><a key={x} href={`#${x}`} className="text-sm capitalize text-[var(--muted)] hover:text-[var(--text)] transition focus-ring">{x}</a>)}</div><div className="flex items-center gap-2"><button onClick={()=>setPalette(true)} className="hidden sm:flex items-center gap-2 rounded-xl border border-[var(--line)] px-3 py-2 text-xs text-[var(--muted)] focus-ring"><Command size={14}/>K</button><button onClick={()=>setLight(v=>!v)} aria-label="Toggle theme" className="rounded-xl border border-[var(--line)] p-2 focus-ring">{light?<Moon size={17}/>:<Sun size={17}/>}</button><button onClick={()=>setOpen(v=>!v)} className="md:hidden rounded-xl border border-[var(--line)] p-2 focus-ring" aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div></nav>{open&&<div className="glass container mt-2 rounded-2xl p-4 md:hidden">{links.map(x=><a onClick={()=>setOpen(false)} key={x} href={`#${x}`} className="block py-3 capitalize">{x}</a>)}</div>}</header>{children}<a href="#top" aria-label="Back to top" className="fixed bottom-5 right-5 z-40 rounded-full glass p-3 focus-ring"><ArrowUp size={18}/></a>{palette&&<div className="fixed inset-0 z-[100] grid place-items-start bg-black/60 p-4 pt-[15vh]" onClick={()=>setPalette(false)}><div onClick={e=>e.stopPropagation()} className="glass mx-auto w-full max-w-xl rounded-3xl p-3"><div className="flex items-center gap-3 border-b border-[var(--line)] p-3"><Command size={18}/><input autoFocus placeholder="Jump to a section…" className="w-full bg-transparent outline-none"/></div>{links.map(x=><a onClick={()=>setPalette(false)} href={`#${x}`} key={x} className="block rounded-xl px-4 py-3 capitalize hover:bg-white/5">Go to {x}</a>)}</div></div>}</>}
